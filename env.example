# API Configuration
NEXT_PUBLIC_API_URL=https://printrove-api.vizdale.com/api

# For local development, use:
# NEXT_PUBLIC_API_URL=http://localhost:5001/api

# Note: AWS credentials are NO LONGER required in the frontend
# File uploads now go through the backend API for security
# AWS credentials should only be configured in the backend environment
